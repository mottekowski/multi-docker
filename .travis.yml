sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t mottekowsi/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true mottekowsi/react-test npm test

after_success:
  - docker build -t mottekowsi/multi-client ./client
  - docker build -t mottekowsi/multi-nginx ./nginx
  - docker build -t mottekowsi/multi-server ./server
  - docker build -t mottekowsi/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push mottekowsi/multi-client
  - docker push mottekowsi/multi-nginx
  - docker push mottekowsi/multi-server
  - docker push mottekowsi/multi-worker
